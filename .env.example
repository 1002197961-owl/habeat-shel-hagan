# The shared song catalog works with built-in originals when these are unset.
# Copy to .env.local after creating the dedicated Habeat Supabase project.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# Never put a service_role or sb_secret key here. Catalog access is governed by RLS.
